module top (
    input  wire        clk,
    input  wire        rst,       
    output reg  [15:0] led
);

    always @(posedge clk) begin    
        if (!rst)                 
            led <= 16'b1;
        else
            led <= {led[14:0], led[15]};  
    end

endmodule
